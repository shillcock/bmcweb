.row
  #clearance.password-reset.col-md-6.col-md-offset-3
    .well
      %h2= t '.title'
      %p= t '.description'
      = simple_form_for :password, url: passwords_path do |f|
        = f.input :email
        = f.submit class: "btn btn-primary"
