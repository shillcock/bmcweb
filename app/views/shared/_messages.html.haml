- flash.each do |name, msg|
  - if msg.is_a?(String)
    .alert{class: "alert-#{name == :notice ? "success" : "warning"} alert-dismissable"}
      %button.close{type: "button", "data-dismiss" => "alert", "aria-hidden" => "true"} &times;
      = msg
