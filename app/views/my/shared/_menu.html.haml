%ul.nav.nav-pills.nav-stacked
  %li{class: ("active" if current == "alumni")}
    = link_to "Alumni Membership", my_alumni_membership_path
  %li{class: ("active" if current == "profile")}
    = link_to "Profile", my_profile_path
  %li{class: ("active" if current == "billing")}
    = link_to "Billing", my_billing_path
  %li{class: ("active" if current == "invoices")}
    = link_to "Invoices", my_invoices_path
