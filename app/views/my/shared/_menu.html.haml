%ul.nav.nav-pills.nav-stacked
  %li{class: ("active" if current == "alumni")}
    = link_to fa_icon("users", text: "Alumni Membership"), my_alumni_membership_path
%hr
%ul.nav.nav-pills.nav-stacked
  %li{class: ("active" if current == "profile")}
    = link_to fa_icon("user", text: "Profile"), my_profile_path
  %li{class: ("active" if current == "account")}
    = link_to fa_icon("key", text: "Account"), my_account_path
  %li{class: ("active" if current == "invoices")}
    = link_to fa_icon("money", text: "Payments"), my_payments_path
