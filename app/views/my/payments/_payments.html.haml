- if not payments.empty?
  %p
    Your payment history is not available. If you have any questions about your account, please email
    #{mail_to "support@breakthroughmenscommunity.org"}.
- else
  %table.table
    %thead
      %tr
        %th Date
        %th Card Details
        %th Amount
    %tbody
      - payments.each do |payment|
        %tr
          %td= payment.date
          %td= "XXXX-XXXX-XXXX-#{payment.card_last4}"
          %td= "$#{payment.amount}"
