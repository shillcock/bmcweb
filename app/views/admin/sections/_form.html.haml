= simple_form_for [:admin, workshop, section] do |f|
  = f.input :title
  = f.simple_fields_for :meetings, section.meetings do |m|
    = render "meeting_fields", f: m, meeting: m.object
  = f.submit class: "button round"
