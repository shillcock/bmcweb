= simple_form_for [:admin, user] do |f|
  = f.input :first_name
  = f.input :last_name
  = f.input :email
  = f.input :password
  = f.input :admin, label: "Is an admin?", wrapper: :vertical_boolean
  = f.submit (user.persisted? ? "Update" : "Create") + " User", class: "btn btn-primary"
