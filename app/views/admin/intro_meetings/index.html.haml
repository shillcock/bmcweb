.row
  .large-3.columns
    %p
      %ul.side-nav
        %li= link_to fa_icon("plus-circle", text: "New Intro Meeting"), new_admin_intro_meeting_path, class: "button secondary round"
  .large-9.columns
    %p
      %table#intro_meetings
        %thead
          %tr
            %th Date
            %th Time
            %th Registrations
            %th Delete
        %tbody
          - @intro_meetings.each do |meeting|
            %tr{id: "intro_meeting_#{meeting.id}"}
              %td= meeting.date
              %td= "#{meeting.starts_at.strftime('%l:%M %p')} - #{meeting.ends_at.strftime('%l:%M %p')}"
              %td
                = link_to admin_intro_meeting_registrations_path(meeting) do
                  Registrations
                  %span.secondary.round.label
                    = meeting.registrations.count
              %td= link_to "Delete", admin_intro_meeting_path(meeting), method: :delete,
                data: { confirm: "Are you sure you want to delete this intro meeting?" }
