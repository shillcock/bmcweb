%table.table.table-hover#meetings
  %thead
    %tr
      %th ID
      %th #
      %th Title
      %th Date
      %th Starts at
      %th Ends at
  %tbody
    - meetings.each do |meeting|
      %tr[meeting]
        %td= meeting.id
        %td= meeting.short_title
        %td= link_to meeting.title, [:admin, workshop, meeting]
        %td= formatted_date(meeting.start_date)
        %td= formatted_time(meeting.start_time)
        %td= formatted_time(meeting.end_time)
