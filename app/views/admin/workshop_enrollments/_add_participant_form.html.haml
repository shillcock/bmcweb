= simple_form_for @enrollment, url: admin_workshop_enrollments_path(@workshop) do |f|
  = f.input :user_id, collection: users, label: false
  = f.input :role, collection: roles, include_blank: false, label: false
  = f.submit "Add to roster", class: "btn btn-primary btn-block"
